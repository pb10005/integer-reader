{
  "name": "integer-reader",
  "version": "0.0.2",
  "description": "Integer Reader is a JS library written in TypeScript which converts an integer into words.",
  "main": "./dist/index.js",
  "repository": "https://github.com/pb10001/integer-reader.git",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "npm run build && mocha --exit",
    "unit": "npm test",
    "testreport": "npm run build && nyc --reporter=html mocha --exit"
  },
  "author": "pb10001",
  "license": "MIT",
  "keywords": [
    "integer"
  ],
  "files": [
    "README.md",
    "index.js",
    "dist",
    "LICENSE",
    "package.json"
  ],
  "devDependencies": {
    "mocha": "^5.2.0",
    "nyc": "^13.1.0",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "@types/node": "^10.12.10"
  }
}
